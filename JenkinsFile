pipeline {
    agent {
    label 'Slave'
    }
    stages {
        stage('Build') { 
            steps {
                echo "Executing build"
		sh 'java -version'
		cd src
		javac HelloWorld.java
		java HelloWorld
		
            }
        }
    }
}
